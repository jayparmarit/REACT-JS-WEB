// old method

const Props = (props) => {
    return (
        <>
            <h1>bapa sitaram</h1>
            <h1>{props.greeting}</h1>
            <h1>{props.count}</h1>
        </>
    )
}

export default Props;